text_box_spawner = instance_create_layer(x, y, layer, oTextBoxSpawner)

text_box_spawner.add_text(
"Got 'Em! Now, use this surprisingly heavy slime to push that there button. That button controls the door (Well, specifically it controls the light above the door, and the door responds to the color of light, but for our intent, just worry about getting the button). However, because its so heavy, pits prove to be detrimental to its health, so avoid those if possible."
)

text_box_spawner.add_text(
"                                   . . . "
)

text_box_spawner.add_text(
"Remember, to leave the slime and return to your initial form, press TAB / (X)"
)
