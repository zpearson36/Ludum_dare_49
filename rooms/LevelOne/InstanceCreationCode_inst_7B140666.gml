text_box_spawner = instance_create_layer(x, y, layer, oTextBoxSpawner)

text_box_spawner.add_text(
"You did it! Now leave that slime behind and lets move to the next room"
)

text_box_spawner.add_text(
"                                   . . . "
)

text_box_spawner.add_text(
"Exit Creature: press TAB / (X)"
)
